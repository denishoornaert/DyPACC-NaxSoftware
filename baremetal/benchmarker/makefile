PROJ_NAME=benchmarker

WORKER_NUM?=7
MAIN_CORE?=0
MAIN_PRIO?=7
SECOND_PRIO?=MAIN_PRIO
SECOND_CORE?=1
N_RUNS?=5
INIT_DELAY?=0
BENCHMARK?=BENCH_C2C
PRIO_SUPPORT?=TRUE
N_PINGS?=100
CFLAGS += -DWORKER_NUM=$(WORKER_NUM) -DMAIN_CORE=$(MAIN_CORE) -DMAIN_PRIO=$(MAIN_PRIO) -DN_RUNS=$(N_RUNS) -DINIT_DELAY=$(INIT_DELAY) -DBENCH=$(BENCHMARK) -DPRIO_SUPPORT=$(PRIO_SUPPORT) -DN_PINGS=$(N_PINGS) -DSECOND_PRIO=$(SECOND_PRIO) -DSECOND_CORE=$(SECOND_CORE)

CFLAGS += -fno-zero-initialized-in-bss

DEBUG=no
BENCH=no
STANDALONE = ..

SRCS = 	$(wildcard src/*.c) \
		$(wildcard src/*.cpp) \
		$(wildcard src/*.S)
#        ${STANDALONE}/common/start.S

RISCV_PATH?=/opt/riscv32/
RISCV_NAME?= riscv32-unknown-elf

MARCH=rv32ima_zicsr

include ../common/app.mk

